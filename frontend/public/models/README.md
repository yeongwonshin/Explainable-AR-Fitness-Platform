# MediaPipe model location

Place `pose_landmarker_lite.task` here for local web inference:

```text
frontend/public/models/pose_landmarker_lite.task
```

The frontend wrapper uses this path by default. You may replace it with a CDN URL or a larger model variant after validating mobile performance.
